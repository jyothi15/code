#include <iostream>
using namespace std;

int main() {
    int result,rev = 0,num,i;
    cout << "enter:";
    cin >> num;
    for(i=num;i!=0;i = i/10){
    num = (i % 10);
    rev = rev * 10 + num ;
    }
  result = rev;
    cout << result;
    return 0;
}
